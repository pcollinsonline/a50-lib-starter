{
  "name": "a50-lib-starter",
  "description": "Modern TypeScript library starter",
  "version": "0.1.0",
  "repository": "https://github.com/pcollinsonline/a50-lib-starter",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "npm run clean && npm run build",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "node --test --loader ts-node/esm src/**/*.test.ts **/*.test.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
